#!/bin/sh
set -e

RELEASE_VERSION=v0.7.1
RELEASE_TITLE="${RELEASE_VERSION} - fix length count with utf characters"
RELEASE_NOTE='bug fixes - fix length count with utf characters'

export RELEASE_VERSION RELEASE_TITLE RELEASE_NOTE
