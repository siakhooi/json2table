#!/bin/sh
set -e

RELEASE_VERSION=v0.5.0
RELEASE_TITLE="${RELEASE_VERSION} - color support"
RELEASE_NOTE='add color support, bug fixed, code refactored'

export RELEASE_VERSION RELEASE_TITLE RELEASE_NOTE
