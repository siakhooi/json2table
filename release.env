#!/bin/sh
set -e

RELEASE_VERSION=v0.6.0
RELEASE_TITLE="${RELEASE_VERSION} - conditional color support"
RELEASE_NOTE='add conditional color support, add values validators, code refactored, unit test'

export RELEASE_VERSION RELEASE_TITLE RELEASE_NOTE
